factories_config = {
  billing_accounts = "null_path" # Or any path that does not contain billing account YAMLs
  # Keep other factory configurations if they are still needed
  cicd             = "datasets/classic/cicd.yaml"
  defaults          = "defaults.yaml"
  folders           = "folders"
  organization      = "organization"
  projects          = "projects"
}
